# Zero Auth Relay Configuration

PORT=3000
NODE_ENV=development

# Redis Connection (Requires local redis or cloud instance)
REDIS_URL=redis://localhost:6374

# Relay Identity
RELAY_DID=did:web:relay.zeroauth.app

# Logging
LOG_LEVEL=info
